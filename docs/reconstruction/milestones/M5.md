# M5 - Tool Broker

## Status

```text
MILESTONE_EXISTENCE: PROVEN
DELIVERABLE_TYPE: IMPLEMENTATION
IMPLEMENTATION_STATUS: PARTIAL
SPEC_CONFORMANCE_STATUS: PARTIAL
HISTORICAL_EXECUTION_EVIDENCE: NOT_RETAINED
ACCEPTANCE_STATUS: NOT_FOUND
RECONSTRUCTION_CONFIDENCE: HIGH
```

## Git Boundary

```text
PREDECESSOR_COMMIT: a958c397d974153d5712ff5bb2e35df7e4966c4f
MILESTONE_FIRST_COMMIT: 6cbade0ea53f1652d44bb92f92831a0c8daf62c5
MILESTONE_LAST_COMMIT: 6cbade0ea53f1652d44bb92f92831a0c8daf62c5
MILESTONE_COMPLETION_COMMIT: 6cbade0ea53f1652d44bb92f92831a0c8daf62c5
SUCCESSOR_START_COMMIT: b0d77e173918d34f10fc9ea7c1802f8bdad12384
SHARED_COMMIT_GROUP: M5_AND_M5_5A
```

The successor-start commit is `b0d77e1`; its abbreviated identity is used in
the surrounding historical narrative.

## Historical Role

M5 introduced a deterministic, run-bound tool boundary above M4: exact tool
registration, explicit typed inputs, task permission checks, immutable call and
result records, and opt-in declared Evidence.

## Actual Historical Result

The shared commit adds the M5 broker, registry, persistence, built-in
calculators, task `allowed_tools`, tool provenance fields, M5 design/plan, and
11 committed M5 tests. It also prematurely imports M5.5A
`BackendProvenance`, although no `backends` package exists in this tree.
Consequently `mechcad_harness.tools` and `mechcad_harness.models` cannot import
at the M5 tree; M5 is committed implementation evidence but not a
self-contained executable milestone.

## Tests Present

`tests/unit/test_tools.py` first appears at M5 with 11 test functions. The
tests cover provenance-only context, exact registration, explicit permission,
call/result immutability, failures, stale task rejection, deterministic
built-ins, and declared-Evidence handling. Committed tests establish test-code
presence only; no historical pass transcript survives.

## Historical Verification / Acceptance

No completion report, review, acceptance record, CI transcript, tag, note, or
test output was retained. The historical M5 plan remains unchecked. The known
missing import prevents this committed tree from collecting its tests, so no
execution success can be inferred.

## Material Deviations

- Shared M5.5A provenance integration references an absent module.
- The broker accepts `PENDING` tasks, bypassing M4 scheduler readiness.
- It checks mutable run binding but does not revalidate the canonical snapshot.
- A persisted successful ToolResult does not retain the later Evidence ID.

## Predecessor / Successor Relationship

M5 follows pure M4 run control. `b0d77e1` adds the missing backend package but
also introduces broader gear/CAD work; it cannot retroactively establish M5
execution or acceptance.

## Later Evolution

The broker and backend boundary were extended by subsequent gear, mediation,
production-orchestration, and candidate-realization work.

## Reconstruction Conclusion

M5 is a proven shared-commit implementation attempt with material integration
defects, no retained historical execution evidence, and no retained acceptance.

See [detailed Git evidence](../evidence/M5_GIT_RECONSTRUCTION.md).
