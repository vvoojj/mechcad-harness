# M7D-1 - EL Kinematic Reference

## Status

```text
MILESTONE_EXISTENCE: PROVEN
DELIVERABLE_TYPE: IMPLEMENTATION
IMPLEMENTATION_STATUS: PRESENT_BUT_UNUSED
SPEC_CONFORMANCE_STATUS: PARTIAL
HISTORICAL_EXECUTION_EVIDENCE: NOT_RETAINED
ACCEPTANCE_STATUS: NOT_FOUND
RECONSTRUCTION_CONFIDENCE: HIGH
```

## Git Boundary

M7D-1 is a logical slice of shared commit
`8079c5764d377df3b182f8ffc72a306a186b57af`, the direct child of
`9ab9e48b8bc54202d5fddd5edc85e7f8c7c3b903` and direct parent of M8C
`6c6f46c06fb0ba624639832c1f7c224d3c34d39b`. M7D-2 is co-delivered in the
same commit. The M7D-1-specific addition is five files and 539 lines.

## Historical Role And Result

M7D-1 adds frozen, strict `YagiELKinematicReference` with fixed axis-height
range `(180.0, 300.0)`, no selected axis height, deterministic hashing, and a
helper that preserves caller-supplied axis, angle, and instance ordering for a
generic `CadKinematicSweepRequest`.

It excludes geometry, mechanism selection, motors, materials, loads, FEA,
state mutation, and continuous verification. No non-test production caller
uses this reference at the boundary.

## Tests And Evidence

Six unit tests are present: three reference tests and three sweep-reference
tests. Exact candidate reproduction yields `6 passed`; a broader generic
kinematic regression scope yields `19 passed`. No target-era test transcript,
live result, generated artifact, or dedicated acceptance record is retained.

The later project description claims reference-scope completion, while the
integration audit records FreeCAD unavailable and the path as
`IMPLEMENTED_BUT_UNUSED`. Those documents do not establish a live production
acceptance run.

## Material Deviations

- The plan helper signature omits source assembly ID/hash; implementation
  requires both explicitly.
- Assembly identity remains separately supplied executable provenance rather
  than being derived from layout identity.
- The helper validates through reference factories but does not apply reference
  transforms despite broader composition wording.
- The fixed tuple enforces exactly `(180.0, 300.0)` rather than validating a
  general finite ascending range.
- No dedicated production caller or M7D-1 completion record exists.

## Successor Relationship

M7D-2 extends this boundary with an EL sweep adapter. `6c6f46c` preserves the
implementation and adds imported-component assemblies and production
kinematics; later M9 evidence belongs to those successor paths.

## Reconstruction Conclusion

M7D-1 is a proven reference-only implementation with six unit tests, no
retained live evidence, and no production caller or formal acceptance.

See [detailed Git evidence](../evidence/M7D-1_GIT_RECONSTRUCTION.md).
