# M6A-1 - Agent Gateway Foundation

## Status

```text
MILESTONE_EXISTENCE: PROVEN
DELIVERABLE_TYPE: IMPLEMENTATION
IMPLEMENTATION_STATUS: PRESENT
SPEC_CONFORMANCE_STATUS: PARTIAL
HISTORICAL_EXECUTION_EVIDENCE: NOT_RETAINED
ACCEPTANCE_STATUS: NOT_FOUND
RECONSTRUCTION_CONFIDENCE: HIGH
```

## Git Boundary

M6A-1 is `e4f4c002365d877e6d39d97b62386b8e51e67d76`, the direct child of
M5.5C `4bc2310` and direct parent of M6A-2B `60ccc2da6f2e6287a8d8b6baa697951f9a669404`.
It changes 15 files with 987 insertions and 1 deletion.

## Historical Role And Result

The commit delivers a deterministic FakeAgent-only boundary: strict agent
identity/context/response/invocation/result/provenance models, exact registry
lookup, minimal persisted context, pre-invocation persistence, canonical
request/response hashes, proposal binding, stale-result handling, and
immutable exclusive-write result storage.

It explicitly excludes OpenCode, LLMs, subprocesses, network, tools, proposal
application, state mutation, and autonomous loops. `config/agents.yaml` remains
`agents: []`; tests manually register `mechcad-test-agent@1.0`.

## Tests And Acceptance

The commit adds 19 test functions: models 4, runtime 3, gateway 7,
authoritative context 4, and documentation 1. No retained test transcript,
completion report, CI artifact, acceptance record, tag, or Git note exists. The
plan's 25 checklist items remain unchecked, so implementation is not
historically acceptance-verified.

## Material Deviations

- Gateway request identity is reconstructed as role `test` and protocol `1.0`
  rather than verified against adapter identity.
- UTC timestamps have UTC defaults but accept caller-supplied naive datetimes.
- Pydantic models are not frozen or assignment-validated; immutability applies
  to persisted exclusive-write records and defensive copied context.
- Deterministic hash repeatability and configured Issue/ConstraintRequest
  responses are not fully exercised.

## Successor Relationship

`60ccc2d` consumes this foundation and adds the deferred real loopback HTTP
OpenCode adapter, execution outcome/error provenance, structured-output and
model-selection validation, plus opt-in live tests. It extends rather than
replaces M6A-1 semantics.

## Reconstruction Conclusion

M6A-1 is a proven FakeAgent-only implementation with partial conformance and no
retained formal acceptance evidence.

See [detailed Git evidence](../evidence/M6A-1_GIT_RECONSTRUCTION.md).
