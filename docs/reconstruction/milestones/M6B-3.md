# M6B-3 - Bounded Constraint Discovery

## Status

```text
MILESTONE_EXISTENCE: PROVEN
DELIVERABLE_TYPE: IMPLEMENTATION
IMPLEMENTATION_STATUS: PRESENT
SPEC_CONFORMANCE_STATUS: PARTIAL
HISTORICAL_EXECUTION_EVIDENCE: NOT_RETAINED
ACCEPTANCE_STATUS: NOT_FOUND
RECONSTRUCTION_CONFIDENCE: HIGH
```

## Git Boundary

M6B-3 is `53fa6c4d356cabbfed5e0fa4b35532a9ca6ba882`, the direct child of M6B
`928be44` and direct parent of M6B-4A `3c7c70883005cbfdc91b985d9a4bc2467fd41326`.
It changes 15 files with 567 insertions and 15 deletions.

## Historical Role And Result

The commit adds four typed discovery keys: output angular speed, motor
characteristics, output interface, and packaging envelope. It adds a typed
constraint-discovery response, tool-forbidden response contract, durable
constraint request/observation records, deterministic `CRREQ-*` IDs, and a
bounded discovery mode in the transmission round trip. Canonical state remains
immutable and no second tool execution is introduced.

## Tests And Execution Evidence

The candidate grows from 237 to 250 top-level test functions, adding 13. The
focused reproduction yielded `63 passed, 1 skipped`; the newly added tests
yielded `12 passed, 1 skipped`. The candidate collected 256 pytest cases. The
full exact-tree reproduction yielded `230 passed, 25 skipped, 1 failed`, with
the inherited py_gearworks availability assertion causing the failure.

The opt-in live discovery test failed at Invocation A with `OpenCode request
failed`. No live acceptance result, CI transcript, completion report, audit
record, tag, or Git note is retained.

## Material Deviations

- `ConstraintRequestMaterializer.is_satisfied()` treats a raw canonical anchor
  as satisfied rather than requiring a matching typed authoritative parameter.
  M6B-4A immediately tightens this rule.
- The OpenCode fallback and live test hard-code `E:/repo/mechcad-harness`.
- No M6B-3-specific spec, plan, README section, or acceptance record exists at
  the candidate boundary.

## Successor Relationship

M6B-4A extracts the keys into `engineering.keys`, adds typed resolution answers
and values, and introduces `DesignState.authoritative_parameters`. M6B-4C
later adds resolution application and workflow closure. These successors
confirm the raw-anchor correction but do not establish M6B-3 acceptance.

## Reconstruction Conclusion

M6B-3 is a proven bounded, unit-tested constraint-discovery implementation that
was not independently live-accepted and whose satisfaction semantics were
immediately tightened by M6B-4A.

See [detailed Git evidence](../evidence/M6B-3_GIT_RECONSTRUCTION.md).
