# M13-4 - Promotion Evidence Contract

## Status

```text
MILESTONE_EXISTENCE: PROVEN
DELIVERABLE_TYPE: IMPLEMENTATION_AND_SYSTEM_ACCEPTANCE
IMPLEMENTATION_STATUS: PRESENT
SPEC_CONFORMANCE_STATUS: ACCEPTED_WITH_LIMITATIONS
HISTORICAL_EXECUTION_EVIDENCE: RETAINED_FOCUSED
ACCEPTANCE_STATUS: M13_4_INDEPENDENT_FINAL_ACCEPTED
RECONSTRUCTION_CONFIDENCE: HIGH
```

## Git Boundary

M13-4 is `185a304796c17793519fb5f01dbf80cca73ab51e`, the direct child of M13-3
`ca294e045f53979cf6bc1d90404888501e50271f`. It changes 35 files with 17,985
insertions and 3 deletions. It bundles M13-4E (multi-joint promotion Evidence
contract), M13-4P (production composition wiring), and the M13-4 representative
live capstone and audits.

## Historical Role And Result

The core contract adds `MultiJointPromotionDecisionInputReference`, which binds
source revision/state, candidate, M12-3 result, evaluation request/result,
selection, scope, configuration set, placement derivations, pair
classifications, M10 request/result, promotion policy, target mechanism, mapping
identities, and classification identities.
`SelectedMultiJointCandidateDecisionManifest` is compact pre-application
provenance preserving the typed compilation and exact candidate-to-canonical
mapping. `MultiJointPromotionResultManifest` binds the decision artifact,
proposal, ChangeSet, changed paths, base revision/state, and actual N+1
revision/state.

Artifacts are deterministic JSON in `ArtifactStore`
(`MULTI-JOINT-PROMOTION-DECISION-{hash}` and
`MULTI-JOINT-PROMOTION-RESULT-{hash}`). The transient application receipt has
no self-hash; trusted verification resolves the durable artifacts instead.
`EvidenceStore` still owns invalidation records and the promotion manifests are
not duplicated into it. Failure statuses distinguish pre-apply failure,
ChangeEngine rejection, partial post-apply failures, and verified promotion.

## Tests And Acceptance

The commit adds eight test files with about 6,225 lines. Retained evidence:
M13-4E R12 re-audit `116 passed` focused, `75 passed` predecessor, full
`2,811 passed, 34 skipped`, with R12-01/R12-02 closed; M13-4P `8 passed`, `116`
M13-4E regression, `75` predecessor, full `2,819 passed, 34 skipped`; the final
M13-4 capstone records a fresh pass with `2,832 passed, 25 skipped` after earlier
intermediate rejection records. The current successor has no committed source
or test diff after this commit and no committed `PRJ-M13*` artifact.

## Material Deviations

- The commit bundles implementation, remediation, independent audits, and later
  acceptance records; earlier `REJECTED`/`PENDING_INDEPENDENT_REAUDIT` markers
  remain alongside later accepted markers and must be read chronologically.
- The terminal acceptance authority is
  `MECHCAD_M13_4_INDEPENDENT_FINAL_ACCEPTANCE.md`
  (`M13_4_INDEPENDENT_FINAL_ACCEPTED`/`ACCEPTED`), not the intermediate
  completion report.
- The rejection chain is preserved in full but was previously under-enumerated.
  Recorded M13-4E sequence: `M13_4E_INDEPENDENT_R12_REJECTED` (CRITICAL R12-01)
  -> `M13_4E_R12_REMEDIATION_READY_FOR_REAUDIT` / `PENDING_INDEPENDENT_REAUDIT`
  -> `M13_4E_INDEPENDENT_R12_ACCEPTED`. Recorded M13-4P sequence:
  `M13_4P_BLOCKED_BY_PROMOTION_EVIDENCE_SCHEMA_GAP` ->
  `M13_4P_NARROW_COMPOSITION_WIRING_REQUIRED` /
  `RECONCILIATION_READY_FOR_INDEPENDENT_AUDIT` -> `M13_4P_INDEPENDENT_ACCEPTED`.
  Recorded M13-4 capstone sequence: `M13_4_INDEPENDENT_REAUDIT_REJECTED` ->
  `M13_4_INDEPENDENT_SECOND_REAUDIT_REJECTED` ->
  `M13_4_INDEPENDENT_THIRD_REAUDIT_REJECTED` / `M13_4_INDEPENDENT_REJECTED` ->
  `M13_4_INDEPENDENT_FINAL_ACCEPTED`. The original obstruction
  `M13_4_BLOCKED_BY_ARCHITECTURE_GAP` is preserved as history.
- The final M13-4 acceptance record states that the governing capstone test
  file was still untracked in a dirty worktree at acceptance time; it was
  committed later in `185a304`. The acceptance is therefore not reproducible
  from the terminal commit's clean checkout without reconstructing that dirty
  state. See `UNRESOLVED_GAPS.md`.

## Structural Boundary

Structural analysis is explicitly excluded: `M11_STATUS = UNRESOLVED`,
`M11_ELIGIBLE = False`, and no structural definition, material, load, mesh,
solver request, or FEA result. M13-4E has no FreeCAD runtime acceptance gate;
structural-profile skips are environmental full-suite skips. Optional M11
handoff fields are provenance/intent binding only.

## Reconstruction Conclusion

M13-4 is a proven bundled promotion-Evidence contract and acceptance closure
with durable deterministic manifests, chronological multi-audit records, and
explicit structural exclusion.

See [detailed Git evidence](../evidence/M13-4_GIT_RECONSTRUCTION.md).
