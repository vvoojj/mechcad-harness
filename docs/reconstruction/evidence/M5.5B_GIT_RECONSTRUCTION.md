# M5.5B Historical Reconstruction

## Verdict

```text
MILESTONE_EXISTENCE: PROVEN
DELIVERABLE_TYPE: IMPLEMENTATION
IMPLEMENTATION_STATUS: PRESENT
SPEC_CONFORMANCE_STATUS: PARTIAL
HISTORICAL_EXECUTION_EVIDENCE: NOT_RETAINED
ACCEPTANCE_STATUS: NOT_FOUND
RECONSTRUCTION_CONFIDENCE: HIGH
RECONSTRUCTION_REVIEW_RESULT: PASS_WITH_NOTES
```

## Boundary

`b0d77e173918d34f10fc9ea7c1802f8bdad12384` is the direct child of
`6cbade0` and the direct parent of `4bc23105ada9244b2403bf95864ededd5587059e`.
It is a bundled delivery rather than a clean B-only commit. M5.5B-2 has the
dedicated spec
`docs/superpowers/specs/2026-08-18-mechcad-m5-5b2-gear-cad-artifacts-design.md`
and plan
`docs/superpowers/plans/2026-08-18-mechcad-m5-5b-2-gear-cad-artifacts.md`.
M5.5B-1 is described in README but has no separate spec or plan in this tree.

## Implemented Artifacts

The commit adds backend models, compatibility/provenance, registry repair,
artifact models/store, a pinned `PyGearworksAdapter`, gear calculations, CAD
builders, and four ToolBroker registrations. Artifact paths are run-scoped
under `projects/<project>/runs/<run>/artifacts/<artifact>/`; canonical
`DesignState` remains separate.

The optional gear profile declares NumPy, SciPy, build123d, and py_gearworks at
the exact revision
`2fc2a13d82a9997a65f30c870498f0bb3be62318`. `GEAR_AVAILABLE` only checks for
py_gearworks, although adapter health checking validates the broader profile.
`packaging` is imported by compatibility code without a direct pyproject
dependency declaration.

## Tests

The added test files contain exactly 20 top-level functions:

| File | Functions |
| --- | ---: |
| `tests/unit/test_artifacts.py` | 5 |
| `tests/unit/test_gear_backend.py` | 10 |
| `tests/unit/test_gear_cad.py` | 5 |

The complete `b0d77e1` snapshot contains 85 test functions. A reproduction of
the M5.5B tests passed all 20. A reproduction of the complete snapshot yielded
84 passed, 1 failed, 0 skipped. The failure is
`test_package_inspection_uses_trusted_distribution_mapping`, whose fixed
expectation of `UNAVAILABLE` conflicts with installed `py_gearworks==0.0.18`.
This is a reproduction, not retained historical execution evidence.

## Conformance And Deviations

The implementation conforms to the scoped artifact hashing, atomic persistence,
lazy third-party import, broker provenance, and canonical-state isolation
intent. It does not provide all planned tests. Specifically, repeated-export
equality, bore-volume reduction, CAD permission/stale/failure/binding checks,
and pair-axis/no-assembly checks are absent.

The pair operation calls the adapter's relative placement logic but exports
the two gears independently and stores only
`relative_transform=(nominal_center_distance, 0.0, 0.0)`. It does not persist
positioned pair geometry. This is a material deviation from the B-2 wording.

## Acceptance

No M5.5B completion report, review, acceptance marker, CI log, test transcript,
tag, or Git note was found. README smoke-test prose is not durable acceptance
evidence.

## Successor

`4bc2310` adds M5.5C section integration and does not modify the M5.5B gear
implementation. Later M6 commits add agent context and an OpenCode gateway;
they do not establish historical M5.5B acceptance.

## Review Conclusion

Skeptical review confirms the shared/bundled boundary, 20 added tests, the
84/1 full-suite reproduction, the pair-transform limitation, and the absence
of retained acceptance evidence.
